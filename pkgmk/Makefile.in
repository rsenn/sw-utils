# Makefile.in generated by automake 1.7.9 from Makefile.am.
# @configure_input@

# Copyright 1994, 1995, 1996, 1997, 1998, 1999, 2000, 2001, 2002, 2003
# Free Software Foundation, Inc.
# This Makefile.in is free software; the Free Software Foundation
# gives unlimited permission to copy and/or distribute it,
# with or without modifications, as long as this notice is preserved.

# This program is distributed in the hope that it will be useful,
# but WITHOUT ANY WARRANTY, to the extent permitted by law; without
# even the implied warranty of MERCHANTABILITY or FITNESS FOR A
# PARTICULAR PURPOSE.

@SET_MAKE@

srcdir = @srcdir@
top_srcdir = @top_srcdir@
VPATH = @srcdir@
pkgdatadir = $(datadir)/@PACKAGE@
pkglibdir = $(libdir)/@PACKAGE@
pkgincludedir = $(includedir)/@PACKAGE@
top_builddir = ..

am__cd = CDPATH="$${ZSH_VERSION+.}$(PATH_SEPARATOR)" && cd
INSTALL = @INSTALL@
install_sh_DATA = $(install_sh) -c -m 644
install_sh_PROGRAM = $(install_sh) -c
install_sh_SCRIPT = $(install_sh) -c
INSTALL_HEADER = $(INSTALL_DATA)
transform = $(program_transform_name)
NORMAL_INSTALL = :
PRE_INSTALL = :
POST_INSTALL = :
NORMAL_UNINSTALL = :
PRE_UNINSTALL = :
POST_UNINSTALL = :
host_triplet = @host@
BUILD_DEBUG = @BUILD_DEBUG@
BUILD_OPT = @BUILD_OPT@
CC = @CC@
CFLAGS = @CFLAGS@
CFLAGS_DEBUG = @CFLAGS_DEBUG@
CFLAGS_OPT = @CFLAGS_OPT@
CPPFLAGS = @CPPFLAGS@
CXX = @CXX@
CXXFLAGS = @CXXFLAGS@
CXXFLAGS_DEBUG = @CXXFLAGS_DEBUG@
CXXFLAGS_OPT = @CXXFLAGS_OPT@
DEBUG = @DEBUG@
DEFS = @DEFS@
ECHO_C = @ECHO_C@
ECHO_N = @ECHO_N@
ECHO_T = @ECHO_T@
EXEEXT = @EXEEXT@
LDFLAGS = @LDFLAGS@
LIBOBJS = @LIBOBJS@
LIBS = @LIBS@
LN_S = @LN_S@
LTLIBOBJS = @LTLIBOBJS@
LTVERSION = @LTVERSION@
OBJEXT = @OBJEXT@
PACKAGE_BUGREPORT = @PACKAGE_BUGREPORT@
PACKAGE_NAME = @PACKAGE_NAME@
PACKAGE_STRING = @PACKAGE_STRING@
PACKAGE_TARNAME = @PACKAGE_TARNAME@
PACKAGE_URL = @PACKAGE_URL@
PACKAGE_VERSION = @PACKAGE_VERSION@
PATH_SEPARATOR = @PATH_SEPARATOR@
PKG_DIR = @PKG_DIR@
SHELL = @SHELL@
WERROR = @WERROR@
ac_ct_CC = @ac_ct_CC@
ac_ct_CXX = @ac_ct_CXX@
bindir = @bindir@
build = @build@
build_alias = @build_alias@
build_cpu = @build_cpu@
build_os = @build_os@
build_vendor = @build_vendor@
datadir = @datadir@
datarootdir = @datarootdir@
docdir = @docdir@
dvidir = @dvidir@
exec_prefix = @exec_prefix@
fakeroot = @fakeroot@
host = @host@
host_alias = @host_alias@
host_cpu = @host_cpu@
host_os = @host_os@
host_vendor = @host_vendor@
htmldir = @htmldir@
includedir = @includedir@
infodir = @infodir@
libdir = @libdir@
libexecdir = @libexecdir@
localedir = @localedir@
localstatedir = @localstatedir@
mandir = @mandir@
oldincludedir = @oldincludedir@
pdfdir = @pdfdir@
prefix = @prefix@
program_transform_name = @program_transform_name@
psdir = @psdir@
rellocalstatedir = @rellocalstatedir@
relprefix = @relprefix@
relsysconfdir = @relsysconfdir@
sbindir = @sbindir@
sharedstatedir = @sharedstatedir@
shlibdir = @shlibdir@
subdirs = @subdirs@
swrel = @swrel@
swroot = @swroot@
sysconfdir = @sysconfdir@
target = @target@
target_alias = @target_alias@
target_cpu = @target_cpu@
target_os = @target_os@
target_vendor = @target_vendor@
SOMESCRIPTS = pkgmk pkgmk-deb

bin_SCRIPTS = $(SOMESCRIPTS)

sysconf_DATA = pkgmk.conf
pkgmksysconfdir = $(sysconfdir)/pkgmk.d
pkgmkconfsysconfdir = $(sysconfdir)/pkgmk.conf.d

pkgmklibdir = $(libdir)/pkgmk
pkgmklib_SCRIPTS = pkgmk-buildcmd

bashcompdir = $(datadir)/bash
bashcomp_DATA = pkgmk.comp

pkgmkconfsysconf_DATA = \
    layout.conf \
    mirrors.conf \
    toolchain.conf


EXTRA_DIST = \
    pkgmk.in \
    pkgmk.8.in \
    pkgmk.conf.in \
    pkgmk-deb.in \
    libtool.conf.in \
    layout.conf.in \
    mirrors.conf.in \
    toolchain.conf.in \
    pkgmk-buildcmd.in \
    pkgmk.comp.in


DISTCLEANFILES = \
    pkgmk.8 \
    pkgmk \
    pkgmk-deb \
    pkgmk.conf \
    $(pkgmkconfsysconf_DATA)


man8_MANS = pkgmk.8

TRACE_BINARIES = \
  cc c++ gcc g++ lcc icl cl icl.exe cl.exe \
  install cp mv rm ln touch mk$(NULL)dir \
  bzip2 bunzip2 bzcat \
  gzip gunzip zcat \
  tar zip unzip rar unrar \
  make gmake pmake smake \
  pkg-config \
  ar as dlltool ld nm objdump objcopy ranlib readelf size strings strip windres

subdir = pkgmk
mkinstalldirs = $(SHELL) $(top_srcdir)/mkinstalldirs
CONFIG_HEADER = $(top_builddir)/config.h
CONFIG_CLEAN_FILES = pkgmk pkgmk-deb pkgmk.conf pkgmk.comp libtool.conf \
	layout.conf mirrors.conf toolchain.conf pkgmk.8 pkgmk-buildcmd
SCRIPTS = $(bin_SCRIPTS) $(pkgmklib_SCRIPTS)

DIST_SOURCES =

NROFF = nroff
MANS = $(man8_MANS)
DATA = $(bashcomp_DATA) $(pkgmkconfsysconf_DATA) $(sysconf_DATA)

DIST_COMMON = $(srcdir)/Makefile.in Makefile.am layout.conf.in \
	libtool.conf.in mirrors.conf.in pkgmk-buildcmd.in pkgmk-deb.in \
	pkgmk.8.in pkgmk.comp.in pkgmk.conf.in pkgmk.in \
	toolchain.conf.in
all: all-am

.SUFFIXES:
$(srcdir)/Makefile.in:  Makefile.am  $(top_srcdir)/configure.in $(ACLOCAL_M4)
	cd $(top_srcdir) && \
	  $(AUTOMAKE) --foreign  pkgmk/Makefile
Makefile:  $(srcdir)/Makefile.in  $(top_builddir)/config.status
	cd $(top_builddir) && $(SHELL) ./config.status $(subdir)/$@ $(am__depfiles_maybe)
pkgmk: $(top_builddir)/config.status pkgmk.in
	cd $(top_builddir) && $(SHELL) ./config.status $(subdir)/$@
pkgmk-deb: $(top_builddir)/config.status pkgmk-deb.in
	cd $(top_builddir) && $(SHELL) ./config.status $(subdir)/$@
pkgmk.conf: $(top_builddir)/config.status pkgmk.conf.in
	cd $(top_builddir) && $(SHELL) ./config.status $(subdir)/$@
pkgmk.comp: $(top_builddir)/config.status pkgmk.comp.in
	cd $(top_builddir) && $(SHELL) ./config.status $(subdir)/$@
libtool.conf: $(top_builddir)/config.status libtool.conf.in
	cd $(top_builddir) && $(SHELL) ./config.status $(subdir)/$@
layout.conf: $(top_builddir)/config.status layout.conf.in
	cd $(top_builddir) && $(SHELL) ./config.status $(subdir)/$@
mirrors.conf: $(top_builddir)/config.status mirrors.conf.in
	cd $(top_builddir) && $(SHELL) ./config.status $(subdir)/$@
toolchain.conf: $(top_builddir)/config.status toolchain.conf.in
	cd $(top_builddir) && $(SHELL) ./config.status $(subdir)/$@
pkgmk.8: $(top_builddir)/config.status pkgmk.8.in
	cd $(top_builddir) && $(SHELL) ./config.status $(subdir)/$@
pkgmk-buildcmd: $(top_builddir)/config.status pkgmk-buildcmd.in
	cd $(top_builddir) && $(SHELL) ./config.status $(subdir)/$@
binSCRIPT_INSTALL = $(INSTALL_SCRIPT)
install-binSCRIPTS: $(bin_SCRIPTS)
	@$(NORMAL_INSTALL)
	$(mkinstalldirs) $(DESTDIR)$(bindir)
	@list='$(bin_SCRIPTS)'; for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  if test -f $$d$$p; then \
	    f=`echo "$$p" | sed 's|^.*/||;$(transform)'`; \
	    echo " $(binSCRIPT_INSTALL) $$d$$p $(DESTDIR)$(bindir)/$$f"; \
	    $(binSCRIPT_INSTALL) $$d$$p $(DESTDIR)$(bindir)/$$f; \
	  else :; fi; \
	done

uninstall-binSCRIPTS:
	@$(NORMAL_UNINSTALL)
	@list='$(bin_SCRIPTS)'; for p in $$list; do \
	  f=`echo "$$p" | sed 's|^.*/||;$(transform)'`; \
	  echo " rm -f $(DESTDIR)$(bindir)/$$f"; \
	  rm -f $(DESTDIR)$(bindir)/$$f; \
	done
pkgmklibSCRIPT_INSTALL = $(INSTALL_SCRIPT)
install-pkgmklibSCRIPTS: $(pkgmklib_SCRIPTS)
	@$(NORMAL_INSTALL)
	$(mkinstalldirs) $(DESTDIR)$(pkgmklibdir)
	@list='$(pkgmklib_SCRIPTS)'; for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  if test -f $$d$$p; then \
	    f=`echo "$$p" | sed 's|^.*/||;$(transform)'`; \
	    echo " $(pkgmklibSCRIPT_INSTALL) $$d$$p $(DESTDIR)$(pkgmklibdir)/$$f"; \
	    $(pkgmklibSCRIPT_INSTALL) $$d$$p $(DESTDIR)$(pkgmklibdir)/$$f; \
	  else :; fi; \
	done

uninstall-pkgmklibSCRIPTS:
	@$(NORMAL_UNINSTALL)
	@list='$(pkgmklib_SCRIPTS)'; for p in $$list; do \
	  f=`echo "$$p" | sed 's|^.*/||;$(transform)'`; \
	  echo " rm -f $(DESTDIR)$(pkgmklibdir)/$$f"; \
	  rm -f $(DESTDIR)$(pkgmklibdir)/$$f; \
	done
uninstall-info-am:

man8dir = $(mandir)/man8
install-man8: $(man8_MANS) $(man_MANS)
	@$(NORMAL_INSTALL)
	$(mkinstalldirs) $(DESTDIR)$(man8dir)
	@list='$(man8_MANS) $(dist_man8_MANS) $(nodist_man8_MANS)'; \
	l2='$(man_MANS) $(dist_man_MANS) $(nodist_man_MANS)'; \
	for i in $$l2; do \
	  case "$$i" in \
	    *.8*) list="$$list $$i" ;; \
	  esac; \
	done; \
	for i in $$list; do \
	  if test -f $(srcdir)/$$i; then file=$(srcdir)/$$i; \
	  else file=$$i; fi; \
	  ext=`echo $$i | sed -e 's/^.*\\.//'`; \
	  case "$$ext" in \
	    8*) ;; \
	    *) ext='8' ;; \
	  esac; \
	  inst=`echo $$i | sed -e 's/\\.[0-9a-z]*$$//'`; \
	  inst=`echo $$inst | sed -e 's/^.*\///'`; \
	  inst=`echo $$inst | sed '$(transform)'`.$$ext; \
	  echo " $(INSTALL_DATA) $$file $(DESTDIR)$(man8dir)/$$inst"; \
	  $(INSTALL_DATA) $$file $(DESTDIR)$(man8dir)/$$inst; \
	done
uninstall-man8:
	@$(NORMAL_UNINSTALL)
	@list='$(man8_MANS) $(dist_man8_MANS) $(nodist_man8_MANS)'; \
	l2='$(man_MANS) $(dist_man_MANS) $(nodist_man_MANS)'; \
	for i in $$l2; do \
	  case "$$i" in \
	    *.8*) list="$$list $$i" ;; \
	  esac; \
	done; \
	for i in $$list; do \
	  ext=`echo $$i | sed -e 's/^.*\\.//'`; \
	  case "$$ext" in \
	    8*) ;; \
	    *) ext='8' ;; \
	  esac; \
	  inst=`echo $$i | sed -e 's/\\.[0-9a-z]*$$//'`; \
	  inst=`echo $$inst | sed -e 's/^.*\///'`; \
	  inst=`echo $$inst | sed '$(transform)'`.$$ext; \
	  echo " rm -f $(DESTDIR)$(man8dir)/$$inst"; \
	  rm -f $(DESTDIR)$(man8dir)/$$inst; \
	done
bashcompDATA_INSTALL = $(INSTALL_DATA)
install-bashcompDATA: $(bashcomp_DATA)
	@$(NORMAL_INSTALL)
	$(mkinstalldirs) $(DESTDIR)$(bashcompdir)
	@list='$(bashcomp_DATA)'; for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  f="`echo $$p | sed -e 's|^.*/||'`"; \
	  echo " $(bashcompDATA_INSTALL) $$d$$p $(DESTDIR)$(bashcompdir)/$$f"; \
	  $(bashcompDATA_INSTALL) $$d$$p $(DESTDIR)$(bashcompdir)/$$f; \
	done

uninstall-bashcompDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(bashcomp_DATA)'; for p in $$list; do \
	  f="`echo $$p | sed -e 's|^.*/||'`"; \
	  echo " rm -f $(DESTDIR)$(bashcompdir)/$$f"; \
	  rm -f $(DESTDIR)$(bashcompdir)/$$f; \
	done
pkgmkconfsysconfDATA_INSTALL = $(INSTALL_DATA)
install-pkgmkconfsysconfDATA: $(pkgmkconfsysconf_DATA)
	@$(NORMAL_INSTALL)
	$(mkinstalldirs) $(DESTDIR)$(pkgmkconfsysconfdir)
	@list='$(pkgmkconfsysconf_DATA)'; for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  f="`echo $$p | sed -e 's|^.*/||'`"; \
	  echo " $(pkgmkconfsysconfDATA_INSTALL) $$d$$p $(DESTDIR)$(pkgmkconfsysconfdir)/$$f"; \
	  $(pkgmkconfsysconfDATA_INSTALL) $$d$$p $(DESTDIR)$(pkgmkconfsysconfdir)/$$f; \
	done

uninstall-pkgmkconfsysconfDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(pkgmkconfsysconf_DATA)'; for p in $$list; do \
	  f="`echo $$p | sed -e 's|^.*/||'`"; \
	  echo " rm -f $(DESTDIR)$(pkgmkconfsysconfdir)/$$f"; \
	  rm -f $(DESTDIR)$(pkgmkconfsysconfdir)/$$f; \
	done
sysconfDATA_INSTALL = $(INSTALL_DATA)
install-sysconfDATA: $(sysconf_DATA)
	@$(NORMAL_INSTALL)
	$(mkinstalldirs) $(DESTDIR)$(sysconfdir)
	@list='$(sysconf_DATA)'; for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  f="`echo $$p | sed -e 's|^.*/||'`"; \
	  echo " $(sysconfDATA_INSTALL) $$d$$p $(DESTDIR)$(sysconfdir)/$$f"; \
	  $(sysconfDATA_INSTALL) $$d$$p $(DESTDIR)$(sysconfdir)/$$f; \
	done

uninstall-sysconfDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(sysconf_DATA)'; for p in $$list; do \
	  f="`echo $$p | sed -e 's|^.*/||'`"; \
	  echo " rm -f $(DESTDIR)$(sysconfdir)/$$f"; \
	  rm -f $(DESTDIR)$(sysconfdir)/$$f; \
	done
tags: TAGS
TAGS:

ctags: CTAGS
CTAGS:

DISTFILES = $(DIST_COMMON) $(DIST_SOURCES) $(TEXINFOS) $(EXTRA_DIST)

top_distdir = ..
distdir = $(top_distdir)/$(PACKAGE)-$(VERSION)

distdir: $(DISTFILES)
	@srcdirstrip=`echo "$(srcdir)" | sed 's|.|.|g'`; \
	topsrcdirstrip=`echo "$(top_srcdir)" | sed 's|.|.|g'`; \
	list='$(DISTFILES)'; for file in $$list; do \
	  case $$file in \
	    $(srcdir)/*) file=`echo "$$file" | sed "s|^$$srcdirstrip/||"`;; \
	    $(top_srcdir)/*) file=`echo "$$file" | sed "s|^$$topsrcdirstrip/|$(top_builddir)/|"`;; \
	  esac; \
	  if test -f $$file || test -d $$file; then d=.; else d=$(srcdir); fi; \
	  dir=`echo "$$file" | sed -e 's,/[^/]*$$,,'`; \
	  if test "$$dir" != "$$file" && test "$$dir" != "."; then \
	    dir="/$$dir"; \
	    $(mkinstalldirs) "$(distdir)$$dir"; \
	  else \
	    dir=''; \
	  fi; \
	  if test -d $$d/$$file; then \
	    if test -d $(srcdir)/$$file && test $$d != $(srcdir); then \
	      cp -pR $(srcdir)/$$file $(distdir)$$dir || exit 1; \
	    fi; \
	    cp -pR $$d/$$file $(distdir)$$dir || exit 1; \
	  else \
	    test -f $(distdir)/$$file \
	    || cp -p $$d/$$file $(distdir)/$$file \
	    || exit 1; \
	  fi; \
	done
check-am: all-am
check: check-am
all-am: Makefile $(SCRIPTS) $(MANS) $(DATA)

installdirs:
	$(mkinstalldirs) $(DESTDIR)$(bindir) $(DESTDIR)$(pkgmklibdir) $(DESTDIR)$(man8dir) $(DESTDIR)$(bashcompdir) $(DESTDIR)$(pkgmkconfsysconfdir) $(DESTDIR)$(sysconfdir)
install: install-am
install-exec: install-exec-am
install-data: install-data-am
uninstall: uninstall-am

install-am: all-am
	@$(MAKE) $(AM_MAKEFLAGS) install-exec-am install-data-am

installcheck: installcheck-am
install-strip:
	$(MAKE) $(AM_MAKEFLAGS) INSTALL_PROGRAM="$(INSTALL_STRIP_PROGRAM)" \
	  install_sh_PROGRAM="$(INSTALL_STRIP_PROGRAM)" INSTALL_STRIP_FLAG=-s \
	  `test -z '$(STRIP)' || \
	    echo "INSTALL_PROGRAM_ENV=STRIPPROG='$(STRIP)'"` install
mostlyclean-generic:

clean-generic:

distclean-generic:
	-rm -f $(CONFIG_CLEAN_FILES)
	-test -z "$(DISTCLEANFILES)" || rm -f $(DISTCLEANFILES)

maintainer-clean-generic:
	@echo "This command is intended for maintainers to use"
	@echo "it deletes files that may require special tools to rebuild."
clean: clean-am

clean-am: clean-generic mostlyclean-am

distclean: distclean-am
	-rm -f Makefile
distclean-am: clean-am distclean-generic

dvi: dvi-am

dvi-am:

info: info-am

info-am:

install-data-am: install-bashcompDATA install-man \
	install-pkgmkconfsysconfDATA install-pkgmklibSCRIPTS
	@$(NORMAL_INSTALL)
	$(MAKE) $(AM_MAKEFLAGS) install-data-hook

install-exec-am: install-binSCRIPTS install-sysconfDATA

install-info: install-info-am

install-man: install-man8

installcheck-am:

maintainer-clean: maintainer-clean-am
	-rm -f Makefile
maintainer-clean-am: distclean-am maintainer-clean-generic

mostlyclean: mostlyclean-am

mostlyclean-am: mostlyclean-generic

pdf: pdf-am

pdf-am:

ps: ps-am

ps-am:

uninstall-am: uninstall-bashcompDATA uninstall-binSCRIPTS \
	uninstall-info-am uninstall-man uninstall-pkgmkconfsysconfDATA \
	uninstall-pkgmklibSCRIPTS uninstall-sysconfDATA

uninstall-man: uninstall-man8

.PHONY: all all-am check check-am clean clean-generic distclean \
	distclean-generic distdir dvi dvi-am info info-am install \
	install-am install-bashcompDATA install-binSCRIPTS install-data \
	install-data-am install-exec install-exec-am install-info \
	install-info-am install-man install-man8 \
	install-pkgmkconfsysconfDATA install-pkgmklibSCRIPTS \
	install-strip install-sysconfDATA installcheck installcheck-am \
	installdirs maintainer-clean maintainer-clean-generic \
	mostlyclean mostlyclean-generic pdf pdf-am ps ps-am uninstall \
	uninstall-am uninstall-bashcompDATA uninstall-binSCRIPTS \
	uninstall-info-am uninstall-man uninstall-man8 \
	uninstall-pkgmkconfsysconfDATA uninstall-pkgmklibSCRIPTS \
	uninstall-sysconfDATA


#  cat cp head ln mv rm tail touch \
#  grep egrep pgrep sed \
#

install-data-hook:
	for name in $(TRACE_BINARIES); do \
    cd $(DESTDIR)$(pkgmklibdir) && ln -sf pkgmk-buildcmd $$name; \
	done
# Tell versions [3.59,3.63) of GNU make to not export all variables.
# Otherwise a system limit (for SysV at least) may be exceeded.
.NOEXPORT:
